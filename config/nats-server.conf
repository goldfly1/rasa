# Rasa Pilot — NATS Server Configuration
port: 4222
http_port: 8222

jetstream {
  store_dir: data/nats
  max_store: 5GB
}

# Authorization (pilot: no auth — localhost only)
authorization: {}

# Limit connections
max_connections: 512

# Logging
log_file: logs/nats.log
log_size_limit: 100MB
